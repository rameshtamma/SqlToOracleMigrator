# Changelog

## Unreleased
- Documentation pack created.

## Known recent changes (from prior work)
- Fixed WWI migration DDL defaults: SQL Server `NEXT VALUE FOR`/`CAST`/`CONVERT` → Oracle-friendly defaults (including `SEQUENCE.NEXTVAL`).
- Enhanced DataDefValidation diagnostics to include generated DDL for better root-cause analysis.
