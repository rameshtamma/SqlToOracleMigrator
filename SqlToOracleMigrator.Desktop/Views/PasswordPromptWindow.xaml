<Window x:Class="SqlToOracleMigrator.Desktop.Views.PasswordPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Credentials Required" Height="240" Width="440"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding PromptText}" FontWeight="SemiBold" Margin="0,0,0,10"/>

        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="User Id" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1" Height="28"
                     Text="{Binding UserId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     IsEnabled="{Binding AllowUserIdEdit}" ToolTip="User name for the selected connection. If enabled, you can edit it to switch to SYS or another account before continuing."/>
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Password" VerticalAlignment="Center"/>
            <PasswordBox Grid.Column="1" x:Name="Pwd" Height="28" ToolTip="Password for the selected connection. Not stored unless the connection is configured to save passwords securely."/>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="OK" Width="90" Margin="0,0,8,0" Click="Ok_Click" ToolTip="Continue using the provided credentials."/>
            <Button Content="Cancel" Width="90" Click="Cancel_Click" ToolTip="Cancel the current action and return without connecting."/>
        </StackPanel>
    </Grid>
</Window>
